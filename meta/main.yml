---
galaxy_info:
  author: Diego Daguerre, Pablo Estigarribia
  description: Install nagios plugins from multiple sources
  license: GPLv2
  min_ansible_version: 2.0
  # https://galaxy.ansible.com/api/v1/platforms/
  platforms:
  - name: Ubuntu
    versions:
    - trusty
  - name: Debian
    versions:
    - jessie
  galaxy_tags:
  - Monitoring
  - Infraestructure
allow_duplicates: no
# Dependencies are only commented due that I prefer to explicit define it on my site.yml
#dependencies:
#  - { role: nagios_server, tags: ["install", "nagios_server_all", "nagios_server"] }
